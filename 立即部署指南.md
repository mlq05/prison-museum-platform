# 🚀 立即部署指南

## 你已经完成的步骤

✅ 已安装 CloudBase CLI  
✅ 已登录

## 现在需要做的

你当前在 `C:\Users\mlq-0` 目录，需要切换到项目目录并执行部署。

### 方法一：使用命令行（复制粘贴执行）

打开命令行（CMD 或 PowerShell），依次执行以下命令：

```cmd
cd F:\test4\服务器
deploy.bat
```

**注意**：
- `#` 开头的是注释，不需要执行
- 只需要执行上面的两行命令

---

### 方法二：直接执行部署命令

如果想直接执行，在命令行中输入：

```cmd
cd /d F:\test4\服务器
cloudbase run:deploy --envId test-7g8oe7lq75832d7c
```

---

## 如果遇到问题

1. **找不到目录**：确认项目路径是 `F:\test4`
2. **命令执行失败**：查看错误信息，告诉我具体的错误
3. **部署失败**：查看云开发控制台的构建日志

---

## 部署成功后的步骤

1. 在云开发控制台获取服务地址
2. 配置环境变量
3. 更新小程序配置

详细步骤见：`完整部署清单.md`

