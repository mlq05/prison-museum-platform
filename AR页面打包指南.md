# 📦 AR页面打包指南（静态网站托管）

## 📋 需要上传的文件

### 文件清单

```
miniprogram/ar-pages/
├── marker-ar-simple.html        ✅ AR页面主文件
└── libs/                        ✅ AR库文件目录
    ├── aframe-ar.js             ✅ AR.js库
    ├── aframe.min.js            ✅ A-Frame库
    ├── camera_para.dat          ✅ 相机参数文件
    └── hiro.patt                ✅ Hiro marker模式文件
```

---

## 🚀 上传步骤

### 方法一：通过云开发控制台上传（推荐）

#### 步骤 1：进入静态网站托管
1. 登录 [云开发控制台](https://console.cloud.tencent.com/tcb)
2. 选择环境：`prison-museum-dev`（或你的环境）
3. 左侧菜单 → **"静态网站托管"**

#### 步骤 2：开通服务（如果未开通）
1. 点击 **"开通静态网站托管"**
2. 等待开通完成（约1-2分钟）

#### 步骤 3：上传文件
1. 点击 **"上传文件"** 或 **"上传文件夹"**
2. 选择 `miniprogram/ar-pages/` 目录
3. 上传所有文件

#### 步骤 4：确认文件结构
上传后，确保文件结构如下：
```
静态网站托管根目录/
└── ar-pages/
    ├── marker-ar-simple.html
    └── libs/
        ├── aframe-ar.js
        ├── aframe.min.js
        ├── camera_para.dat
        └── hiro.patt
```

#### 步骤 5：获取访问地址
1. 在静态网站托管页面查看 **"默认域名"**
2. 格式如：`https://prison-museum-dev-xxxxx.tcloudbaseapp.com`
3. **AR页面访问地址**：
   ```
   https://prison-museum-dev-xxxxx.tcloudbaseapp.com/ar-pages/marker-ar-simple.html
   ```

---

### 方法二：使用 CloudBase CLI 上传

#### 步骤 1：安装 CLI
```bash
npm install -g @cloudbase/cli
```

#### 步骤 2：登录
```bash
cloudbase login
```

#### 步骤 3：上传
```bash
cd miniprogram/ar-pages
cloudbase hosting:deploy . --envId prison-museum-dev-8e6hujc6eb768b
```

---

## ✅ 上传检查清单

- [ ] 已开通静态网站托管
- [ ] 已上传 `marker-ar-simple.html`
- [ ] 已上传 `libs/` 目录
- [ ] 已上传所有库文件（aframe-ar.js, aframe.min.js, camera_para.dat, hiro.patt）
- [ ] 文件结构正确
- [ ] 已获取访问地址
- [ ] 已测试访问（在浏览器中打开AR页面URL）

---

## 🔧 更新小程序配置

上传成功后，更新小程序代码：

### 文件：`miniprogram/pages/ar-link/ar-link.ts`

**位置**：第 117 行

**修改为**：
```typescript
const cloudHostingUrl = 'https://prison-museum-dev-xxxxx.tcloudbaseapp.com';
```

⚠️ **注意**：将 `prison-museum-dev-xxxxx` 替换为你的实际静态网站托管域名。

---

## 🧪 测试AR页面

### 1. 浏览器测试
在浏览器中访问：
```
https://prison-museum-dev-xxxxx.tcloudbaseapp.com/ar-pages/marker-ar-simple.html?hallId=1
```

应该能看到AR页面加载。

### 2. 小程序测试
1. 在小程序中进入"AR体验"
2. 选择展区
3. 应该能看到AR链接页面
4. 复制链接到浏览器测试

---

## 📝 注意事项

1. **文件路径**：确保文件路径正确，`libs/` 目录必须在 `ar-pages/` 目录下
2. **文件大小**：单个文件建议 < 10MB
3. **访问权限**：静态网站托管默认是公开访问
4. **HTTPS**：静态网站托管自动提供HTTPS

---

## 🆘 常见问题

### Q: 上传后无法访问？
**A**: 检查：
- 文件路径是否正确
- 域名是否正确
- 文件是否已上传成功

### Q: AR页面加载失败？
**A**: 检查：
- 所有库文件是否都已上传
- 文件路径是否正确
- 浏览器控制台是否有错误

### Q: 小程序中无法打开AR链接？
**A**: 检查：
- 小程序代码中的URL是否正确
- 是否已更新 `ar-link.ts` 中的域名

---

## 📚 相关文档

- 静态网站托管文档：https://cloud.tencent.com/document/product/876/40270
- AR页面使用说明：`docs/README.md`

